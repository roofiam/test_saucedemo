services:
  tests:
    build: .

    user: "${UID:-1000}:${GID:-1000}"

    env_file:
      - .env.docker

    environment:
      UV_PROJECT_ENVIRONMENT: /tmp/.venv
      UV_CACHE_DIR: /tmp/uv-cache
    volumes:
      - .:/app

    command: uv run pytest
